################################################################################
#                     DO NOT EDIT THIS FILE (Auto-Generated)                   #
# Contents of this file were generated by https://github.com/parcelLab/.github #
# Changes to this file may be overwritten.                                     #
################################################################################
name: ~Lib / PR
on:
  workflow_call:
    inputs:
      nodeVersion:
        required: false
        description: The node version to provide (e.g. `lts/*`, `18`, `18.4`...)
        default: latest
        type: string
      labelerConfig:
        required: false
        description: The folder that holds the danger package.json (for codebases that want to manage their own danger package.json and configs)
        default: .github/workflows/configs/managed/labeler.yaml
        type: string
jobs:
  danger:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout current git repository
        uses: actions/checkout@v3
      - name: Use Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ inputs.nodeVersion }}
      - name: Install npm dependencies
        run: npm i
        env:
          NPM_GITHUB_TOKEN: ${{ secrets.PACKAGES_READ_TOKEN }}
      - name: Run Danger
        run: node_modules/.bin/danger ci -d dangerfile.cjs
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_ACCESS_TOKEN }}
  label:
    permissions:
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    steps:
      - name: Label PR based on pushed file paths
        continue-on-error: true
        uses: actions/labeler@v4
        with:
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          configuration-path: ${{ inputs.labelerConfig }}
          sync-labels: true
