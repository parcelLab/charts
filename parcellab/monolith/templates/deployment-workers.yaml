{{- $root := . -}}
{{- $defaultWorker := .Values.worker}}
{{- if .Values.workers -}}
{{- range .Values.workers }}
{{- include "common.deployment" (merge (deepCopy $root) (dict "service" (merge $defaultWorker .) "type" "worker")) }}
---
{{- end -}}
{{- end -}}
