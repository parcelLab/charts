{{- if .Values.podDisruptionBudget.enabled -}}
{{- $root := . -}}
{{- $pdbData := dict "podDisruptionBudget" .Values.podDisruptionBudget "name" (include "common.fullname" $root) -}}
{{- include "common.poddisruptionbudget" (merge (deepCopy $root) $pdbData) }}
{{- end -}}
